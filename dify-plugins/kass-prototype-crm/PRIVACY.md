# Privacy

This plugin sends only the `workspace_id`, `customer_ref`, and explicitly requested prototype CRM fields to the fixed KASS prototype API.

It does not accept or transmit Yingdan access tokens, cookies, account credentials, or production CRM data.
