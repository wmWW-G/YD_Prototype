# 编码助手配置

## 项目总管理

本目录维护「赢单」应用的 UI 原型、浏览器插件内测包和 AI 工作流资料。

`AGENTS.md` 只放项目级协作规则和边界；具体路由、状态字段、页面结构、文件职责、验证步骤和历史上下文放在 `CONTEXT.md`。真实实现始终以当前代码为准。

## 工作方式

- 使用中文交流，简洁直接，代码变更为主，文字说明为辅。
- 修改前先读相关代码和 `CONTEXT.md`，不要凭印象改。
- 优先编辑现有文件，不主动创建新文档；项目事实变化时同步更新 `CONTEXT.md`。
- 用户是产品经理、原型图 UI 设计者和 AI 工作流设计者；页面调整通常代表产品判断，优先尊重用户的业务决策。
- 用户不是专业开发者，代码注释要清楚，复杂逻辑要解释“为什么这样做”。
- 创建正式前后端项目或复杂功能前，先询问前端和后端技术栈，并用简洁中文说明利弊。
- 完成代码修改后提醒：`记得提交 git 喔`
- 本项目向 GitHub 提交/推送时必须使用 `github-b` SSH alias（例如 `git@github-b:wmWW-G/YD_Prototype.git`），不要改用默认 `github.com` SSH 地址或 HTTPS。
- 如果通过 `github-b` 提交/推送失败，优先提醒用户检查 VPN 是否开启了 TUN 模式；TUN 很可能是连接失败的原因。未经用户明确同意，不要擅自修改 VPN、TUN、代理或网络配置。

## 编辑边界

- 主静态原型优先改 `index.html`、`src/data.js`、`src/app.js`、`src/styles.css`。
- 浏览器插件优先改 `browser-extension/` 下的现有文件。
- Coze/扣子资料统一维护在 `coze-workflows/`。
- Dify Chatflow 资料统一维护在 `dify-chatflows/`。
- 不要把接口文档、schema、调用函数、设计说明或开发说明写进用户可见界面。
- `.claude/`、`audits/`、`workbench/`、`backups/` 默认不是主工程编辑目标，不主动提交。

## 安全边界

- 不要写入、复述或提交真实 Token、Cookie、API Key、手机号、邮箱、账号信息和客户隐私。
- 不要复制线上真实历史记录到原型数据里。
- 不要点击真实网页里的删除、发送、保存账号设置、修改权限等高风险动作，除非用户明确确认。
- 浏览器插件当前可真实调用 Coze，但内置 Token 只能作为内部测试例外；公开上架前必须移除。
- `赢单api.md` 只是接口文档快照；判断线上行为、鉴权状态或敏感字段暴露时必须重新 live 验证。

## 质量要求

- 页面必须像真实产品一样可操作，不做功能说明页。
- 发送、上传、导出、删除、保存、支付等动作默认只做原型反馈，不能产生真实副作用。
- 当前例外：客户背调顾问已通过“不需要知识库的总库”和通用 Dify 流式代理做真实调用；细节看 `CONTEXT.md`、`dify-chatflows/Chatflow-不需要知识库的总库/`、`api/dify-chat.js` 和 `cloudflare-worker/dify-chat-worker.mjs`。
- 修改主原型后，至少打开页面检查关键流程、文字是否溢出、按钮是否可点、窄屏是否正常。
- 修改浏览器插件后，执行 `package.json` 里的插件检查脚本或按 `CONTEXT.md` 的验证方式逐项检查。
